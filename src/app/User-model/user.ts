export class User {
    constructor(public location: string, public name: string, public avatar_url: string, public company: string,
        public login: string, public bio: string, ) {
    }
}

//public bio: string, public public_repos: number, public login:string, public avatar_url:string, public created_at:Date, public updated_at:Date, public html_url: string
